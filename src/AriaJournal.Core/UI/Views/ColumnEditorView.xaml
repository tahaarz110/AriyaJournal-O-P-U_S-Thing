<!-- =============================================================================
فایل: src/AriaJournal.Core/UI/Views/ColumnEditorView.xaml
توضیح: پنل GUI برای مدیریت و سفارشی‌سازی ستون‌های جدول - اصلاح‌شده
============================================================================= -->

<UserControl x:Class="AriaJournal.Core.UI.Views.ColumnEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             FlowDirection="RightToLeft"
             Background="{DynamicResource BackgroundBrush}"
             d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- هدر -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="📊 مدیریت ستون‌ها"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="{DynamicResource ForegroundBrush}"/>
            <TextBlock Text="ستون‌های جدول را مدیریت کنید: نمایش/مخفی، ترتیب، عرض و عنوان"
                       FontSize="13"
                       Foreground="{DynamicResource SecondaryForegroundBrush}"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- انتخاب جدول -->
        <Border Grid.Row="1"
                Background="{DynamicResource CardBackgroundBrush}"
                CornerRadius="8"
                Padding="15"
                Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="جدول:"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource ForegroundBrush}"
                           Margin="0,0,10,0"/>

                <ComboBox Grid.Column="1"
                          ItemsSource="{Binding AvailableTables}"
                          SelectedItem="{Binding SelectedTable}"
                          DisplayMemberPath="TitleFa"
                          Style="{DynamicResource ComboBoxStyle}"/>

                <Button Grid.Column="2"
                        Content="🔄 بارگذاری"
                        Command="{Binding RefreshCommand}"
                        Style="{DynamicResource SecondaryButtonStyle}"
                        Margin="15,0,0,0"/>

                <StackPanel Grid.Column="3" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Left"
                            Margin="20,0,0,0">
                    <CheckBox Content="فقط ستون‌های نمایان"
                              IsChecked="{Binding ShowOnlyVisibleColumns}"
                              Foreground="{DynamicResource ForegroundBrush}"
                              VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- لیست ستون‌ها -->
        <Border Grid.Row="2"
                Background="{DynamicResource CardBackgroundBrush}"
                CornerRadius="8"
                Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- نوار ابزار -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <Button Content="✅ نمایش همه"
                            Command="{Binding ShowAllCommand}"
                            Style="{DynamicResource SecondaryButtonStyle}"
                            Margin="0,0,10,0"/>
                    <Button Content="❌ مخفی همه"
                            Command="{Binding HideAllCommand}"
                            Style="{DynamicResource SecondaryButtonStyle}"
                            Margin="0,0,10,0"/>
                    <Button Content="↩️ بازنشانی"
                            Command="{Binding ResetCommand}"
                            Style="{DynamicResource SecondaryButtonStyle}"/>

                    <Separator Margin="20,0"/>

                    <TextBlock Text="جستجو:"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundBrush}"
                               Margin="0,0,10,0"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Width="200"
                             Style="{DynamicResource TextBoxStyle}"/>
                </StackPanel>

                <!-- لیست ستون‌ها -->
                <ListView Grid.Row="1"
                          ItemsSource="{Binding Columns}"
                          SelectedItem="{Binding SelectedColumn}"
                          Background="Transparent"
                          BorderThickness="0"
                          AllowDrop="True">
                    <ListView.View>
                        <GridView>
                            <!-- چک‌باکس نمایش -->
                            <GridViewColumn Width="50">
                                <GridViewColumn.Header>
                                    <CheckBox IsChecked="{Binding DataContext.IsAllSelected, RelativeSource={RelativeSource AncestorType=ListView}}"
                                              ToolTip="انتخاب/عدم انتخاب همه"/>
                                </GridViewColumn.Header>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding Visible}"
                                                  ToolTip="نمایش/مخفی"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <!-- نام ستون -->
                            <GridViewColumn Header="نام ستون" Width="180">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Icon}"
                                                       Margin="0,0,5,0"
                                                       VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding HeaderFa}"
                                                       Foreground="{DynamicResource ForegroundBrush}"
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <!-- فیلد -->
                            <GridViewColumn Header="فیلد" Width="120">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Field}"
                                                   Foreground="{DynamicResource SecondaryForegroundBrush}"
                                                   FontFamily="Consolas"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <!-- نوع -->
                            <GridViewColumn Header="نوع" Width="80">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding TypeDisplay}"
                                                   Foreground="{DynamicResource SecondaryForegroundBrush}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <!-- عرض -->
                            <GridViewColumn Header="عرض" Width="100">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Width, UpdateSourceTrigger=PropertyChanged}"
                                                 Width="60"
                                                 Style="{DynamicResource TextBoxStyle}"
                                                 HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <!-- ترتیب -->
                            <GridViewColumn Header="ترتیب" Width="80">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="▲"
                                                    Command="{Binding DataContext.MoveUpCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{DynamicResource IconButtonStyle}"
                                                    Width="26" Height="26"
                                                    ToolTip="بالا"/>
                                            <Button Content="▼"
                                                    Command="{Binding DataContext.MoveDownCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{DynamicResource IconButtonStyle}"
                                                    Width="26" Height="26"
                                                    ToolTip="پایین"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <!-- مرتب‌سازی -->
                            <GridViewColumn Header="مرتب‌سازی" Width="70">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding Sortable}"
                                                  HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <!-- فیلتر -->
                            <GridViewColumn Header="فیلتر" Width="60">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding Filterable}"
                                                  HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <!-- ویرایش -->
                            <GridViewColumn Header="" Width="50">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="✏️"
                                                Command="{Binding DataContext.EditColumnCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                CommandParameter="{Binding}"
                                                Style="{DynamicResource IconButtonStyle}"
                                                ToolTip="ویرایش پیشرفته"
                                                Width="30" Height="30"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>

        <!-- دکمه‌های پایین -->
        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- اطلاعات -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="تعداد ستون‌ها:"
                           Foreground="{DynamicResource SecondaryForegroundBrush}"
                           Margin="0,0,5,0"/>
                <TextBlock Text="{Binding Columns.Count}"
                           Foreground="{DynamicResource ForegroundBrush}"
                           FontWeight="Bold"
                           Margin="0,0,15,0"/>

                <TextBlock Text="نمایان:"
                           Foreground="{DynamicResource SecondaryForegroundBrush}"
                           Margin="0,0,5,0"/>
                <TextBlock Text="{Binding VisibleCount}"
                           Foreground="{DynamicResource SuccessBrush}"
                           FontWeight="Bold"/>

                <Border Background="{DynamicResource WarningBrush}"
                        CornerRadius="10"
                        Padding="8,2"
                        Margin="20,0,0,0"
                        Visibility="{Binding HasChanges, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="تغییرات ذخیره نشده"
                               Foreground="White"
                               FontSize="11"/>
                </Border>
            </StackPanel>

            <!-- دکمه‌ها -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="💾 ذخیره تغییرات"
                        Command="{Binding SaveCommand}"
                        Style="{DynamicResource PrimaryButtonStyle}"
                        Width="150"
                        Margin="0,0,10,0"/>
                <Button Content="انصراف"
                        Command="{Binding CancelCommand}"
                        Style="{DynamicResource SecondaryButtonStyle}"
                        Width="100"/>
            </StackPanel>
        </Grid>

        <!-- پنل ویرایش پیشرفته -->
        <Border Grid.Row="0" Grid.RowSpan="4"
                Background="#80000000"
                Visibility="{Binding IsEditPanelVisible, Converter={StaticResource BoolToVis}}">
            <Border Background="{DynamicResource CardBackgroundBrush}"
                    CornerRadius="10"
                    Padding="25"
                    MaxWidth="450"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="{Binding EditPanelTitle}"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="{DynamicResource ForegroundBrush}"
                               Margin="0,0,0,20"/>

                    <StackPanel Grid.Row="1">
                        <!-- عنوان سفارشی -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="عنوان نمایشی"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,5"/>
                            <TextBox Text="{Binding EditingColumn.CustomHeader, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{DynamicResource TextBoxStyle}"/>
                        </StackPanel>

                        <!-- عرض -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="عرض (پیکسل)"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,5"/>
                            <TextBox Text="{Binding EditingColumn.Width, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{DynamicResource TextBoxStyle}"
                                     Width="100"
                                     HorizontalAlignment="Right"/>
                        </StackPanel>

                        <!-- تراز -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="تراز"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,5"/>
                            <ComboBox SelectedItem="{Binding EditingColumn.Alignment}"
                                      Style="{DynamicResource ComboBoxStyle}">
                                <ComboBoxItem Content="راست"/>
                                <ComboBoxItem Content="چپ"/>
                                <ComboBoxItem Content="وسط"/>
                            </ComboBox>
                        </StackPanel>

                        <!-- فرمت -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="فرمت نمایش"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,5"/>
                            <TextBox Text="{Binding EditingColumn.Format, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{DynamicResource TextBoxStyle}"/>
                            <TextBlock Text="مثال: N2, yyyy/MM/dd"
                                       FontSize="11"
                                       Foreground="{DynamicResource SecondaryForegroundBrush}"
                                       Margin="0,3,0,0"/>
                        </StackPanel>

                        <!-- تنظیمات -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <CheckBox Content="مرتب‌سازی"
                                      IsChecked="{Binding EditingColumn.Sortable}"
                                      Foreground="{DynamicResource ForegroundBrush}"
                                      Margin="0,0,20,0"/>
                            <CheckBox Content="فیلتر"
                                      IsChecked="{Binding EditingColumn.Filterable}"
                                      Foreground="{DynamicResource ForegroundBrush}"
                                      Margin="0,0,20,0"/>
                            <CheckBox Content="تغییر اندازه"
                                      IsChecked="{Binding EditingColumn.Resizable}"
                                      Foreground="{DynamicResource ForegroundBrush}"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Row="2"
                                Orientation="Horizontal"
                                HorizontalAlignment="Left"
                                Margin="0,20,0,0">
                        <Button Content="✔️ تأیید"
                                Command="{Binding ConfirmEditCommand}"
                                Style="{DynamicResource PrimaryButtonStyle}"
                                Width="100"
                                Margin="0,0,10,0"/>
                        <Button Content="انصراف"
                                Command="{Binding CancelEditCommand}"
                                Style="{DynamicResource SecondaryButtonStyle}"
                                Width="100"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Border>
    </Grid>
</UserControl>

<!-- =============================================================================
پایان فایل: src/AriaJournal.Core/UI/Views/ColumnEditorView.xaml
============================================================================= -->