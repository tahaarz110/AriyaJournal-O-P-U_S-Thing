<!-- =============================================================================
فایل: src/AriaJournal.Core/UI/Views/DashboardView.xaml
توضیح: صفحه داشبورد اصلی با ویجت‌های داینامیک
============================================================================= -->

<UserControl x:Class="AriaJournal.Core.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:AriaJournal.Core.UI.Controls"
             FlowDirection="RightToLeft"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- هدر -->
        <Border Grid.Row="0"
                Background="{DynamicResource CardBackgroundBrush}"
                Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="📊 داشبورد"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBlock Text="{Binding LastUpdateText}"
                               FontSize="12"
                               Foreground="{DynamicResource SecondaryForegroundBrush}"
                               Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    
                    <!-- فیلتر بازه زمانی -->
                    <ComboBox ItemsSource="{Binding TimeRanges}"
                              SelectedItem="{Binding SelectedTimeRange}"
                              Width="150"
                              Margin="0,0,15,0"/>

                    <!-- بروزرسانی -->
                    <Button Command="{Binding RefreshCommand}"
                            ToolTip="بروزرسانی"
                            Style="{DynamicResource SecondaryButtonStyle}"
                            Padding="12,8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔄" FontSize="14"/>
                            <TextBlock Text="بروزرسانی" Margin="8,0,0,0"/>
                        </StackPanel>
                    </Button>

                    <!-- تنظیمات داشبورد -->
                    <Button Command="{Binding CustomizeCommand}"
                            ToolTip="سفارشی‌سازی داشبورد"
                            Style="{DynamicResource IconButtonStyle}"
                            Margin="10,0,0,0"
                            Padding="10,8">
                        <TextBlock Text="⚙️" FontSize="16"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- KPI Cards -->
        <Border Grid.Row="1"
                Padding="20,15">
            <ItemsControl ItemsSource="{Binding KpiCards}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="1"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource CardBackgroundBrush}"
                                CornerRadius="8"
                                Padding="20"
                                Margin="0,0,15,0">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" 
                                                  BlurRadius="8" 
                                                  Opacity="0.1"/>
                            </Border.Effect>
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Icon}"
                                               FontSize="24"
                                               Margin="0,0,10,0"/>
                                    <TextBlock Text="{Binding Title}"
                                               FontSize="14"
                                               Foreground="{DynamicResource SecondaryForegroundBrush}"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="{Binding Value}"
                                           FontSize="28"
                                           FontWeight="Bold"
                                           Foreground="{Binding ValueColor}"
                                           Margin="0,10,0,0"/>
                                <TextBlock Text="{Binding Subtitle}"
                                           FontSize="12"
                                           Foreground="{DynamicResource SecondaryForegroundBrush}"
                                           Margin="0,5,0,0"
                                           Visibility="{Binding HasSubtitle, Converter={StaticResource BoolToVis}}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <!-- ویجت‌ها -->
        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl x:Name="WidgetsContainer"
                          ItemsSource="{Binding Widgets}"
                          Margin="20,0,20,20">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:DynamicWidget Width="{Binding Width}"
                                                Height="{Binding Height}"
                                                Margin="0,0,15,15"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- لودینگ -->
        <Border Grid.RowSpan="3"
                Background="#80000000"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
            <StackPanel VerticalAlignment="Center"
                        HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True"
                             Width="200"
                             Height="4"/>
                <TextBlock Text="در حال بارگذاری داشبورد..."
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,15,0,0"
                           FontSize="14"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

<!-- =============================================================================
پایان فایل: src/AriaJournal.Core/UI/Views/DashboardView.xaml
============================================================================= -->