<!-- =============================================================================
فایل: src/AriaJournal.Core/UI/Views/FilterBuilderView.xaml
توضیح: صفحه ساخت و مدیریت فیلترها
============================================================================= -->

<UserControl x:Class="AriaJournal.Core.UI.Views.FilterBuilderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- هدر -->
        <Border Grid.Row="0"
                Background="{DynamicResource CardBackgroundBrush}"
                CornerRadius="8"
                Padding="20"
                Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="🔍 ساخت فیلتر"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                    <TextBlock Text="فیلترهای سفارشی برای جستجوی معاملات ایجاد کنید"
                               FontSize="13"
                               Foreground="{DynamicResource SecondaryForegroundBrush}"
                               Margin="0,5,0,0"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="◀ بازگشت"
                        Command="{Binding GoBackCommand}"
                        Style="{DynamicResource SecondaryButtonStyle}"/>
            </Grid>
        </Border>

        <!-- محتوا -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- لیست فیلترهای ذخیره‌شده -->
            <Border Grid.Column="0"
                    Background="{DynamicResource CardBackgroundBrush}"
                    CornerRadius="8"
                    Padding="15"
                    Margin="0,0,15,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="📁 فیلترهای ذخیره‌شده"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource ForegroundBrush}"
                               Margin="0,0,0,15"/>

                    <ListBox Grid.Row="1"
                             ItemsSource="{Binding SavedFilters}"
                             SelectedItem="{Binding SelectedFilter}"
                             Background="Transparent"
                             BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="10,8"
                                        Margin="0,0,0,5"
                                        CornerRadius="5"
                                        Background="{DynamicResource HoverBrush}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding Icon}"
                                                   FontSize="16"
                                                   Margin="0,0,10,0"/>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding Name}"
                                                       FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Description}"
                                                       FontSize="11"
                                                       Foreground="{DynamicResource SecondaryForegroundBrush}"/>
                                        </StackPanel>
                                        <Button Grid.Column="2"
                                                Content="🗑️"
                                                Command="{Binding DataContext.DeleteFilterCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Style="{DynamicResource IconButtonStyle}"
                                                Visibility="{Binding IsBuiltIn, Converter={StaticResource BoolToVis}, ConverterParameter=Inverse}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- ویرایشگر فیلتر -->
            <Border Grid.Column="1"
                    Background="{DynamicResource CardBackgroundBrush}"
                    CornerRadius="8"
                    Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- نام فیلتر -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,20">
                        <TextBlock Text="نام فیلتر"
                                   Foreground="{DynamicResource ForegroundBrush}"
                                   Margin="0,0,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox Grid.Column="0"
                                      ItemsSource="{Binding FilterIcons}"
                                      SelectedItem="{Binding SelectedIcon}"
                                      Width="60"
                                      Margin="0,0,10,0"/>
                            <TextBox Grid.Column="1"
                                     Text="{Binding FilterName, UpdateSourceTrigger=PropertyChanged}"
                                     Placeholder="نام فیلتر را وارد کنید"/>
                        </Grid>
                    </StackPanel>

                    <!-- افزودن شرط -->
                    <Border Grid.Row="1"
                            Background="{DynamicResource BackgroundBrush}"
                            CornerRadius="5"
                            Padding="15"
                            Margin="0,0,0,15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- فیلد -->
                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <TextBlock Text="فیلد" FontSize="11" Margin="0,0,0,5"
                                           Foreground="{DynamicResource SecondaryForegroundBrush}"/>
                                <ComboBox ItemsSource="{Binding AvailableFields}"
                                          SelectedItem="{Binding SelectedField}"
                                          DisplayMemberPath="LabelFa"/>
                            </StackPanel>

                            <!-- عملگر -->
                            <StackPanel Grid.Column="1" Margin="0,0,10,0">
                                <TextBlock Text="عملگر" FontSize="11" Margin="0,0,0,5"
                                           Foreground="{DynamicResource SecondaryForegroundBrush}"/>
                                <ComboBox ItemsSource="{Binding AvailableOperators}"
                                          SelectedItem="{Binding SelectedOperator}"
                                          DisplayMemberPath="DisplayName"/>
                            </StackPanel>

                            <!-- مقدار -->
                            <StackPanel Grid.Column="2" Margin="0,0,10,0">
                                <TextBlock Text="مقدار" FontSize="11" Margin="0,0,0,5"
                                           Foreground="{DynamicResource SecondaryForegroundBrush}"/>
                                <TextBox Text="{Binding ConditionValue, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>

                            <!-- دکمه افزودن -->
                            <Button Grid.Column="3"
                                    Content="➕ افزودن"
                                    Command="{Binding AddConditionCommand}"
                                    Style="{DynamicResource PrimaryButtonStyle}"
                                    VerticalAlignment="Bottom"
                                    Padding="15,8"/>
                        </Grid>
                    </Border>

                    <!-- لیست شروط -->
                    <ScrollViewer Grid.Row="2">
                        <ItemsControl ItemsSource="{Binding Conditions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource BackgroundBrush}"
                                            CornerRadius="5"
                                            Padding="15"
                                            Margin="0,0,0,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <ComboBox Grid.Column="0"
                                                      ItemsSource="{Binding DataContext.LogicOperators, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                      SelectedItem="{Binding Logic}"
                                                      Width="80"
                                                      Margin="0,0,15,0"
                                                      Visibility="{Binding IsFirst, Converter={StaticResource BoolToVis}, ConverterParameter=Inverse}"/>

                                            <TextBlock Grid.Column="1"
                                                       VerticalAlignment="Center">
                                                <Run Text="{Binding FieldLabel}" FontWeight="SemiBold"/>
                                                <Run Text="{Binding OperatorLabel}"/>
                                                <Run Text="{Binding Value}" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}"/>
                                            </TextBlock>

                                            <Button Grid.Column="2"
                                                    Content="✕"
                                                    Command="{Binding DataContext.RemoveConditionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{DynamicResource IconButtonStyle}"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- دکمه‌ها -->
                    <StackPanel Grid.Row="3"
                                Orientation="Horizontal"
                                HorizontalAlignment="Left"
                                Margin="0,15,0,0">
                        <Button Content="ذخیره فیلتر"
                                Command="{Binding SaveFilterCommand}"
                                Style="{DynamicResource PrimaryButtonStyle}"
                                Width="120"
                                Margin="0,0,10,0"/>
                        <Button Content="اعمال"
                                Command="{Binding ApplyFilterCommand}"
                                Style="{DynamicResource SecondaryButtonStyle}"
                                Width="100"
                                Margin="0,0,10,0"/>
                        <Button Content="پاک کردن"
                                Command="{Binding ClearCommand}"
                                Style="{DynamicResource SecondaryButtonStyle}"
                                Width="100"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>

<!-- =============================================================================
پایان فایل: src/AriaJournal.Core/UI/Views/FilterBuilderView.xaml
============================================================================= -->