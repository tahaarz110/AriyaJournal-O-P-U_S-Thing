<!-- ═══════════════════════════════════════════════════════════════════════ -->
<!-- فایل: RegisterView.xaml -->
<!-- مسیر: src/AriaJournal.Core/UI/Views/RegisterView.xaml -->
<!-- توضیح: صفحه ثبت‌نام کاربر جدید -->
<!-- ═══════════════════════════════════════════════════════════════════════ -->

<Window x:Class="AriaJournal.Core.UI.Views.RegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ثبت‌نام در آریا ژورنال"
        Height="600" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft"
        Background="{DynamicResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- هدر -->
        <Border Grid.Row="0" 
                Background="{DynamicResource PrimaryBrush}" 
                Padding="20">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="📝" 
                           FontSize="48" 
                           HorizontalAlignment="Center"/>
                <TextBlock Text="ثبت‌نام در آریا ژورنال" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,10,0,0"/>
                <TextBlock Text="حساب کاربری جدید ایجاد کنید" 
                           FontSize="14" 
                           Foreground="#E0E0E0"
                           HorizontalAlignment="Center"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- فرم ثبت‌نام -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto"
                      Padding="30">
            <StackPanel>
                <!-- نام کاربری -->
                <TextBlock Text="نام کاربری:" 
                           FontWeight="SemiBold"
                           Margin="0,0,0,5"/>
                <TextBox x:Name="UsernameTextBox"
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         Style="{DynamicResource ModernTextBox}"
                         Padding="12"
                         FontSize="14"/>
                <TextBlock Text="حداقل ۳ کاراکتر" 
                           FontSize="11" 
                           Foreground="Gray"
                           Margin="0,3,0,15"/>

                <!-- رمز عبور -->
                <TextBlock Text="رمز عبور:" 
                           FontWeight="SemiBold"
                           Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox"
                             Style="{DynamicResource ModernPasswordBox}"
                             Padding="12"
                             FontSize="14"
                             PasswordChanged="PasswordBox_PasswordChanged"/>
                <TextBlock Text="حداقل ۶ کاراکتر" 
                           FontSize="11" 
                           Foreground="Gray"
                           Margin="0,3,0,15"/>

                <!-- تکرار رمز عبور -->
                <TextBlock Text="تکرار رمز عبور:" 
                           FontWeight="SemiBold"
                           Margin="0,0,0,5"/>
                <PasswordBox x:Name="ConfirmPasswordBox"
                             Style="{DynamicResource ModernPasswordBox}"
                             Padding="12"
                             FontSize="14"
                             PasswordChanged="ConfirmPasswordBox_PasswordChanged"/>
                <TextBlock Text="رمز عبور را مجدداً وارد کنید" 
                           FontSize="11" 
                           Foreground="Gray"
                           Margin="0,3,0,20"/>

                <!-- پیام خطا -->
                <Border Background="#FFEBEE" 
                        CornerRadius="5" 
                        Padding="10"
                        Margin="0,0,0,15"
                        Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="{Binding ErrorMessage}" 
                               Foreground="#C62828" 
                               TextWrapping="Wrap"/>
                </Border>

                <!-- پیام موفقیت -->
                <Border Background="#E8F5E9" 
                        CornerRadius="5" 
                        Padding="10"
                        Margin="0,0,0,15"
                        Visibility="{Binding HasSuccess, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="{Binding SuccessMessage}" 
                               Foreground="#2E7D32" 
                               TextWrapping="Wrap"/>
                </Border>

                <!-- نمایش Recovery Key -->
                <Border Background="#FFF3E0" 
                        CornerRadius="5" 
                        Padding="15"
                        Margin="0,0,0,15"
                        Visibility="{Binding ShowRecoveryKey, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="⚠️ کلید بازیابی" 
                                   FontWeight="Bold" 
                                   Foreground="#E65100"
                                   Margin="0,0,0,10"/>
                        <TextBlock Text="این کلید را در جای امن ذخیره کنید. بدون این کلید امکان بازیابی رمز عبور وجود ندارد:"
                                   TextWrapping="Wrap"
                                   Foreground="#BF360C"
                                   Margin="0,0,0,10"/>
                        <Border Background="White" 
                                CornerRadius="3" 
                                Padding="10">
                            <TextBox Text="{Binding RecoveryKey, Mode=OneWay}"
                                     IsReadOnly="True"
                                     FontFamily="Consolas"
                                     FontSize="12"
                                     TextWrapping="Wrap"
                                     BorderThickness="0"/>
                        </Border>
                        <Button Content="📋 کپی کلید"
                                Command="{Binding CopyRecoveryKeyCommand}"
                                Style="{DynamicResource SecondaryButton}"
                                Margin="0,10,0,0"
                                HorizontalAlignment="Left"/>
                    </StackPanel>
                </Border>

                <!-- دکمه‌ها -->
                <Button Content="ثبت‌نام"
                        Command="{Binding RegisterCommand}"
                        Style="{DynamicResource PrimaryButton}"
                        IsEnabled="{Binding CanRegister}"
                        Padding="15"
                        FontSize="16"
                        Margin="0,10,0,0"/>

                <Button Content="بازگشت به صفحه ورود"
                        Command="{Binding GoToLoginCommand}"
                        Style="{DynamicResource LinkButton}"
                        Margin="0,15,0,0"
                        HorizontalAlignment="Center"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Loading -->
        <Grid Grid.RowSpan="3" 
              Background="#80000000"
              Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" 
                        VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" 
                             Width="200" 
                             Height="4"/>
                <TextBlock Text="لطفاً صبر کنید..." 
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>

<!-- ═══════════════════════════════════════════════════════════════════════ -->
<!-- پایان فایل: RegisterView.xaml -->
<!-- ═══════════════════════════════════════════════════════════════════════ -->