<!-- =============================================================================
فایل: src/AriaJournal.Core/UI/Views/TradeEntryView.xaml
شماره فایل: 85
توضیح: صفحه ثبت/ویرایش معامله
============================================================================= -->

<UserControl x:Class="AriaJournal.Core.UI.Views.TradeEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- هدر -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="{Binding PageTitle}"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="◀ بازگشت"
                        Command="{Binding GoBackCommand}"
                        Style="{DynamicResource SecondaryButtonStyle}"/>
            </Grid>

            <!-- پیام‌ها -->
            <StackPanel Grid.Row="1">
                <Border Background="#FFEBEE"
                        CornerRadius="5"
                        Padding="10"
                        Margin="0,0,0,10"
                        Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="{Binding ErrorMessage}"
                               Foreground="#C62828"
                               TextWrapping="Wrap"/>
                </Border>

                <Border Background="#E8F5E9"
                        CornerRadius="5"
                        Padding="10"
                        Margin="0,0,0,10"
                        Visibility="{Binding HasSuccess, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="{Binding SuccessMessage}"
                               Foreground="#2E7D32"
                               TextWrapping="Wrap"/>
                </Border>
            </StackPanel>

            <!-- فرم -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- ستون چپ -->
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    
                    <!-- اطلاعات پایه -->
                    <Border Background="{DynamicResource CardBackgroundBrush}"
                            CornerRadius="8"
                            Padding="20"
                            Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="📝 اطلاعات پایه"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,15"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- نماد -->
                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,15">
                                    <TextBlock Text="نماد *" 
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               Margin="0,0,0,5"/>
                                    <TextBox Text="{Binding Symbol, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{DynamicResource TextBoxStyle}"
                                             CharacterCasing="Upper"/>
                                </StackPanel>

                                <!-- جهت -->
                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,15">
                                    <TextBlock Text="جهت *" 
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               Margin="0,0,0,5"/>
                                    <ComboBox SelectedIndex="{Binding DirectionIndex}"
                                              Style="{DynamicResource ComboBoxStyle}">
                                        <ComboBoxItem Content="▲ خرید (Buy)"/>
                                        <ComboBoxItem Content="▼ فروش (Sell)"/>
                                    </ComboBox>
                                </StackPanel>

                                <!-- حجم -->
                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,10,15">
                                    <TextBlock Text="حجم (لات) *" 
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               Margin="0,0,0,5"/>
                                    <TextBox Text="{Binding Volume, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{DynamicResource TextBoxStyle}"/>
                                </StackPanel>

                                <!-- قیمت ورود -->
                                <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,15">
                                    <TextBlock Text="قیمت ورود *" 
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               Margin="0,0,0,5"/>
                                    <TextBox Text="{Binding EntryPrice, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{DynamicResource TextBoxStyle}"/>
                                </StackPanel>

                                <!-- تاریخ ورود -->
                                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock Text="تاریخ ورود *" 
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               Margin="0,0,0,5"/>
                                    <DatePicker SelectedDate="{Binding EntryDate}"
                                                Style="{DynamicResource DatePickerStyle}"/>
                                </StackPanel>

                                <!-- ساعت ورود -->
                                <StackPanel Grid.Row="2" Grid.Column="1">
                                    <TextBlock Text="ساعت ورود" 
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               Margin="0,0,0,5"/>
                                    <TextBox Text="{Binding EntryTimeText, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{DynamicResource TextBoxStyle}"
                                             ToolTip="فرمت: HH:mm"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- سطوح قیمتی -->
                    <Border Background="{DynamicResource CardBackgroundBrush}"
                            CornerRadius="8"
                            Padding="20"
                            Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="📊 سطوح قیمتی"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,15"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- حد ضرر -->
                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,15">
                                    <TextBlock Text="حد ضرر (SL)" 
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               Margin="0,0,0,5"/>
                                    <TextBox Text="{Binding StopLoss, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{DynamicResource TextBoxStyle}"/>
                                </StackPanel>

                                <!-- حد سود -->
                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,15">
                                    <TextBlock Text="حد سود (TP)" 
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               Margin="0,0,0,5"/>
                                    <TextBox Text="{Binding TakeProfit, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{DynamicResource TextBoxStyle}"/>
                                </StackPanel>

                                <!-- قیمت خروج -->
                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock Text="قیمت خروج" 
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               Margin="0,0,0,5"/>
                                    <TextBox Text="{Binding ExitPrice, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{DynamicResource TextBoxStyle}"/>
                                </StackPanel>

                                <!-- تاریخ خروج -->
                                <StackPanel Grid.Row="1" Grid.Column="1">
                                    <TextBlock Text="تاریخ خروج" 
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               Margin="0,0,0,5"/>
                                    <DatePicker SelectedDate="{Binding ExitDate}"
                                                Style="{DynamicResource DatePickerStyle}"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- هزینه‌ها و نتیجه -->
                    <Border Background="{DynamicResource CardBackgroundBrush}"
                            CornerRadius="8"
                            Padding="20">
                        <StackPanel>
                            <TextBlock Text="💰 هزینه‌ها و نتیجه"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,15"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock Text="کمیسیون" 
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               Margin="0,0,0,5"/>
                                    <TextBox Text="{Binding Commission, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{DynamicResource TextBoxStyle}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                                    <TextBlock Text="سواپ" 
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               Margin="0,0,0,5"/>
                                    <TextBox Text="{Binding Swap, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{DynamicResource TextBoxStyle}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="سود/زیان" 
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               Margin="0,0,0,5"/>
                                    <TextBox Text="{Binding ProfitLossDisplay, Mode=OneWay}"
                                             Style="{DynamicResource TextBoxStyle}"
                                             IsReadOnly="True"
                                             FontWeight="Bold"
                                             Foreground="{Binding ProfitLossColor}"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- ستون راست -->
                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    
                    <!-- یادداشت‌ها -->
                    <Border Background="{DynamicResource CardBackgroundBrush}"
                            CornerRadius="8"
                            Padding="20"
                            Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="📝 یادداشت‌ها"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,15"/>

                            <TextBlock Text="دلیل ورود" 
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,5"/>
                            <TextBox Text="{Binding EntryReason, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{DynamicResource TextBoxStyle}"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     Height="70"
                                     VerticalScrollBarVisibility="Auto"
                                     Margin="0,0,0,15"/>

                            <TextBlock Text="یادداشت قبل از معامله" 
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,5"/>
                            <TextBox Text="{Binding PreTradeNotes, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{DynamicResource TextBoxStyle}"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     Height="70"
                                     VerticalScrollBarVisibility="Auto"
                                     Margin="0,0,0,15"/>

                            <TextBlock Text="یادداشت بعد از معامله" 
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,5"/>
                            <TextBox Text="{Binding PostTradeNotes, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{DynamicResource TextBoxStyle}"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     Height="70"
                                     VerticalScrollBarVisibility="Auto"/>
                        </StackPanel>
                    </Border>

                    <!-- ارزیابی -->
                    <Border Background="{DynamicResource CardBackgroundBrush}"
                            CornerRadius="8"
                            Padding="20"
                            Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="⭐ ارزیابی"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,15"/>

                            <TextBlock Text="امتیاز اجرا" 
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,5"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                <RadioButton Content="⭐" Tag="1" GroupName="Rating"
                                             IsChecked="{Binding Rating1}" 
                                             Style="{DynamicResource RatingRadioStyle}"
                                             Margin="0,0,5,0"/>
                                <RadioButton Content="⭐⭐" Tag="2" GroupName="Rating"
                                             IsChecked="{Binding Rating2}" 
                                             Style="{DynamicResource RatingRadioStyle}"
                                             Margin="0,0,5,0"/>
                                <RadioButton Content="⭐⭐⭐" Tag="3" GroupName="Rating"
                                             IsChecked="{Binding Rating3}" 
                                             Style="{DynamicResource RatingRadioStyle}"
                                             Margin="0,0,5,0"/>
                                <RadioButton Content="⭐⭐⭐⭐" Tag="4" GroupName="Rating"
                                             IsChecked="{Binding Rating4}" 
                                             Style="{DynamicResource RatingRadioStyle}"
                                             Margin="0,0,5,0"/>
                                <RadioButton Content="⭐⭐⭐⭐⭐" Tag="5" GroupName="Rating"
                                             IsChecked="{Binding Rating5}" 
                                             Style="{DynamicResource RatingRadioStyle}"/>
                            </StackPanel>

                            <CheckBox Content="طبق پلن معاملاتی بود"
                                      IsChecked="{Binding FollowedPlan}"
                                      Foreground="{DynamicResource ForegroundBrush}"
                                      Margin="0,0,0,10"/>

                            <CheckBox Content="ورود هیجانی/احساسی بود"
                                      IsChecked="{Binding IsImpulsive}"
                                      Foreground="{DynamicResource ForegroundBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- اشتباهات و درس‌ها -->
                    <Border Background="{DynamicResource CardBackgroundBrush}"
                            CornerRadius="8"
                            Padding="20">
                        <StackPanel>
                            <TextBlock Text="📚 اشتباهات و درس‌ها"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,15"/>

                            <TextBlock Text="اشتباهات" 
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Mistakes, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{DynamicResource TextBoxStyle}"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     Height="60"
                                     VerticalScrollBarVisibility="Auto"
                                     Margin="0,0,0,15"/>

                            <TextBlock Text="درس‌های آموخته شده" 
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Lessons, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{DynamicResource TextBoxStyle}"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     Height="60"
                                     VerticalScrollBarVisibility="Auto"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- دکمه‌های عملیات -->
            <Border Grid.Row="3"
                    Background="{DynamicResource CardBackgroundBrush}"
                    CornerRadius="8"
                    Padding="20"
                    Margin="0,20,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- چک‌باکس بستن معامله -->
                    <CheckBox Grid.Column="0"
                              Content="معامله بسته شده است"
                              IsChecked="{Binding IsClosed}"
                              Foreground="{DynamicResource ForegroundBrush}"
                              VerticalAlignment="Center"
                              FontSize="14"/>

                    <!-- دکمه‌ها -->
                    <StackPanel Grid.Column="1" 
                                Orientation="Horizontal">
                        
                        <Button Content="انصراف"
                                Command="{Binding CancelCommand}"
                                Style="{DynamicResource SecondaryButtonStyle}"
                                Width="100"
                                Margin="0,0,10,0"/>

                        <Button Content="ذخیره و جدید"
                                Command="{Binding SaveAndNewCommand}"
                                Style="{DynamicResource SecondaryButtonStyle}"
                                Width="120"
                                Margin="0,0,10,0"
                                Visibility="{Binding IsNewTrade, Converter={StaticResource BoolToVis}}"/>

                        <Button Content="{Binding SaveButtonText}"
                                Command="{Binding SaveCommand}"
                                Style="{DynamicResource PrimaryButtonStyle}"
                                Width="120"
                                IsEnabled="{Binding IsNotBusy}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- لودینگ -->
            <Border Grid.RowSpan="4"
                    Background="#80000000"
                    Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
                <StackPanel VerticalAlignment="Center"
                            HorizontalAlignment="Center">
                    <ProgressBar IsIndeterminate="True"
                                 Width="200"
                                 Height="4"/>
                    <TextBlock Text="در حال ذخیره..."
                               Foreground="White"
                               HorizontalAlignment="Center"
                               Margin="0,15,0,0"
                               FontSize="14"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>

<!-- =============================================================================
پایان فایل: src/AriaJournal.Core/UI/Views/TradeEntryView.xaml
============================================================================= -->