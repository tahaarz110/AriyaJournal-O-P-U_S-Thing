<!-- =============================================================================
فایل: src/AriaJournal.Core/UI/Controls/LoadingIndicator.xaml
شماره فایل: 100
توضیح: کنترل نمایش لودینگ
============================================================================= -->

<UserControl x:Class="AriaJournal.Core.UI.Controls.LoadingIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <Grid>
        <Border Background="#80000000"
                CornerRadius="10"
                Padding="30,20"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel>
                <ProgressBar x:Name="ProgressBar"
                             IsIndeterminate="True"
                             Width="150"
                             Height="4"
                             Foreground="{DynamicResource PrimaryBrush}"/>
                <TextBlock x:Name="MessageText"
                           Text="در حال بارگذاری..."
                           Foreground="White"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           Margin="0,15,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

<!-- =============================================================================
پایان فایل: src/AriaJournal.Core/UI/Controls/LoadingIndicator.xaml
============================================================================= -->